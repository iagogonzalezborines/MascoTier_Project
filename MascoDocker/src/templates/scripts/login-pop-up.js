let button = document.getElementById("pop-up");

button.addEventListener("click",()=>{
   
        // Abrir una nueva ventana con la página HTML
        var popup = window.open("login.html", "popup", "width=600,height=400");
      
});
